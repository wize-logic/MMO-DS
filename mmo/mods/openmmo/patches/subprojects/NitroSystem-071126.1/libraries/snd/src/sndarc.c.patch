--- a/subprojects/NitroSystem-071126.1/libraries/snd/src/sndarc.c
+++ b/subprojects/NitroSystem-071126.1/libraries/snd/src/sndarc.c
@@ -86,7 +86,12 @@
     if (!result) return;
 
     FS_InitFile(&arc->file);
-    result = FS_OpenFileFast(&arc->file, arc->fileId);
+    /* FS_OpenFile, not FS_OpenFileFast: the fast path takes a file id, so a
+     * file the mod filesystem replaces is invisible to it and the archive is
+     * always the image's own. Opening BY PATH is what lets a package supply a
+     * sound archive at all; with no replacement FS_OpenFile is this same pair
+     * of calls, so nothing changes for a stock boot. */
+    result = FS_OpenFile(&arc->file, filePath);
     NNS_ASSERTMSG(result, "Cannot open file %s\n", filePath);
     if (!result) return;
 
@@ -115,7 +120,12 @@
     if (!result) return FALSE;
 
     FS_InitFile(&arc->file);
-    result = FS_OpenFileFast(&arc->file, arc->fileId);
+    /* FS_OpenFile, not FS_OpenFileFast: the fast path takes a file id, so a
+     * file the mod filesystem replaces is invisible to it and the archive is
+     * always the image's own. Opening BY PATH is what lets a package supply a
+     * sound archive at all; with no replacement FS_OpenFile is this same pair
+     * of calls, so nothing changes for a stock boot. */
+    result = FS_OpenFile(&arc->file, filePath);
     if (!result) return FALSE;
 
     arc->file_open = TRUE;
