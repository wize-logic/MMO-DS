--- a/src/overlay005/field_camera.c
+++ b/src/overlay005/field_camera.c
@@ -7,6 +7,20 @@
 #include "camera.h"
 #include "math_util.h"
 
+/* openmmo: how far back the host holds the field camera, and the look-at
+ * offset a ported template carries (mods/openmmo/src/openmmo_camera.c). */
+extern void openmmo_camera_scale_distance(fx32 *distance, fx32 *farPlane);
+extern void openmmo_camera_offset_lookat(Camera *camera, int index);
+
+/* openmmo: HeartGold's seventeen field camera templates ride after this game's
+ * seventeen, and a ported header names its own (mmo/tools/portmap.py,
+ * PL_CAMERA_TYPES). The rows are the source's overlay 1 table (ov01_02206478)
+ * field for field. The first twenty-four bytes of a row ARE this struct,
+ * which is why its room camera equals INTERIOR_ORTHOGRAPHIC below and its last
+ * entry equals DEFAULT. Generated by mmo/tools/gen_cameras.py --c, held to
+ * mmo/CAMERAS by tests/mapformat_test.sh. */
+#define OPENMMO_PORTED_CAMERA_FIRST 17
+
 // History size must be greater than the delay, so we add 1 to the delay to get the minimum acceptable size.
 #define FIELD_CAMERA_DELAY        6
 #define FIELD_CAMERA_HISTORY_SIZE (FIELD_CAMERA_DELAY + 1)
@@ -157,20 +171,42 @@
         .nearPlaneDist = CAMERA_DEFAULT_NEAR_CLIP,
         .farPlaneDist = CAMERA_DEFAULT_FAR_CLIP,
     },
+    [OPENMMO_PORTED_CAMERA_FIRST + 0] = { 2731713, { 0xDD62, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 1] = { 1656412, { 0xE383, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x0981, 548864, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 2] = { 2731713, { 0xE3C2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 3] = { 2731713, { 0xF242, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 4] = { 6404251, { 0xDC82, 0x0000, 0x0000 }, CAMERA_PROJECTION_ORTHOGRAPHIC, 0x0281, 614400, 7106560 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 5] = { 1907190, { 0xDDA2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x0881, 614400, 4751360 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 6] = { 2731713, { 0xDFE2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 6144000 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 7] = { 2731713, { 0xE1E2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 8] = { 2111308, { 0xD922, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x0770, 614400, 3686400 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 9] = { 2735809, { 0xD582, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 10] = { 1296389, { 0xDF42, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x0C81, 614400, 6963200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 11] = { 2186281, { 0xE3C2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x0741, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 12] = { 2731713, { 0xDFE2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 6963200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 13] = { 2731713, { 0xF242, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 6963200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 14] = { 2731713, { 0xD4C2, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 4915200 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 15] = { 6404251, { 0xDC82, 0x0000, 0x0000 }, CAMERA_PROJECTION_ORTHOGRAPHIC, 0x0281, 614400, 7106560 },
+    [OPENMMO_PORTED_CAMERA_FIRST + 16] = { 2731713, { 0xD602, 0x0000, 0x0000 }, CAMERA_PROJECTION_PERSPECTIVE, 0x05C1, 614400, 3686400 },
 };
 
 void FieldCamera_Create(const VecFx32 *_target, FieldSystem *fieldSystem, const enum CameraType configID, const BOOL withHistory)
 {
     const VecFx32 *target = _target;
     const CameraSettings *config = &sCameraTypes[configID];
+    fx32 distance = config->distance;
+    fx32 farPlane = config->farPlaneDist;
 
     GF_ASSERT(configID < NELEMS(sCameraTypes));
 
+    openmmo_camera_scale_distance(&distance, &farPlane);
+
     fieldSystem->camera = Camera_Alloc(HEAP_ID_FIELD1);
 
-    Camera_InitWithTarget(target, config->distance, &config->cameraAngle, config->verticalFov, config->projection, TRUE, fieldSystem->camera);
+    Camera_InitWithTarget(target, distance, &config->cameraAngle, config->verticalFov, config->projection, TRUE, fieldSystem->camera);
+    openmmo_camera_offset_lookat(fieldSystem->camera, configID - OPENMMO_PORTED_CAMERA_FIRST);
     Camera_SetAsActive(fieldSystem->camera);
-    Camera_SetClipping(config->nearPlaneDist, config->farPlaneDist, fieldSystem->camera);
+    Camera_SetClipping(config->nearPlaneDist, farPlane, fieldSystem->camera);
 
     if (withHistory) {
         Camera_InitHistory(FIELD_CAMERA_HISTORY_SIZE, FIELD_CAMERA_DELAY, CAMERA_DELAY_Y, HEAP_ID_FIELD1, fieldSystem->camera);
