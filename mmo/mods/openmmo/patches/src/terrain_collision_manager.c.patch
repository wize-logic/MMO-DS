--- a/src/terrain_collision_manager.c	2026-09-01 02:43:35.908972459 -0400
+++ b/src/terrain_collision_manager.c	2026-09-01 02:43:47.169753287 -0400
@@ -18,6 +18,9 @@
 #include "map_tile_behavior.h"
 #include "terrain_attributes.h"
 
+// Which of this game's own props a carried one stands for (openmmo_propequiv.c).
+extern int openmmo_prop_equiv(int modelID);
+
 #define INVALID_TILE_ATTRIBUTES 0xFF
 #define INVALID_TILE_BEHAVIOR   0xFF
 
@@ -429,7 +432,7 @@
                 int mapPropModelID = MapProp_GetModelID(mapProp);
 
                 for (u8 k = 0; k < mapPropModelIDsCount; k++) {
-                    if (mapPropModelID == mapPropModelIDs[k]) {
+                    if (openmmo_prop_equiv(mapPropModelID) == mapPropModelIDs[k]) {
                         if (mapPropOut != NULL) {
                             *mapPropOut = mapProp;
                         }
@@ -462,7 +465,9 @@
             MapProp *mapProp = MapPropManager_GetLoadedProp(mapPropMan, j);
             int mapPropModelID = MapProp_GetModelID(mapProp);
 
-            if (mapPropModelID == targetMapPropModelID) {
+            // A carried prop may BE the one the search names, wearing
+            // another game's art (openmmo_propequiv.c).
+            if (openmmo_prop_equiv(mapPropModelID) == targetMapPropModelID) {
                 if (mapPropOut != NULL) {
                     *mapPropOut = mapProp;
                 }
@@ -494,7 +499,9 @@
             int mapPropModelID = MapProp_GetModelID(mapProp);
 
             for (u8 k = 0; k < mapPropModelIDsCount; k++) {
-                if (mapPropModelID == mapPropModelIDs[k]) {
+                // The third of these searches, and the one the storage
+                // terminal uses (openmmo_propequiv.c).
+                if (openmmo_prop_equiv(mapPropModelID) == mapPropModelIDs[k]) {
                     if (mapPropOut != NULL) {
                         *mapPropOut = mapProp;
                     }
