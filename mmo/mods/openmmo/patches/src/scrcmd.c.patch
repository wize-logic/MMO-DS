--- a/src/scrcmd.c
+++ b/src/scrcmd.c
@@ -816,64 +816,65 @@
 
 static BOOL ScrCmd_Unused_004(ScriptContext *ctx)
 {
-    u8 v0 = ScriptContext_ReadByte(ctx);
-
-    ctx->data[v0] = ScriptContext_ReadByte(ctx);
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 599 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_599(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_599(ctx);
 }
 
 static BOOL ScrCmd_Unused_005(ScriptContext *ctx)
 {
-    u8 v0 = ScriptContext_ReadByte(ctx);
-    u32 v1 = ScriptContext_ReadWord(ctx);
-    ctx->data[v0] = v1;
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 600 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_600(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_600(ctx);
 }
 
 static BOOL ScrCmd_Unused_006(ScriptContext *ctx)
 {
-    u8 v0 = ScriptContext_ReadByte(ctx);
-    u8 *v1 = (u8 *)ScriptContext_ReadWord(ctx);
-    ctx->data[v0] = *v1;
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 601 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_601(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_601(ctx);
 }
 
 static BOOL ScrCmd_Unused_007(ScriptContext *ctx)
 {
-    u8 *v0 = (u8 *)ScriptContext_ReadWord(ctx);
-    u8 v1 = ScriptContext_ReadByte(ctx);
-    *v0 = v1;
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 602 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_602(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_602(ctx);
 }
 
 static BOOL ScrCmd_Unused_008(ScriptContext *ctx)
 {
-    u8 *v0 = (u8 *)ScriptContext_ReadWord(ctx);
-    u8 v1 = ScriptContext_ReadByte(ctx);
-    *v0 = ctx->data[v1];
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 603 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_603(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_603(ctx);
 }
 
 static BOOL ScrCmd_Unused_009(ScriptContext *ctx)
 {
-    u8 v0 = ScriptContext_ReadByte(ctx);
-    u8 v1 = ScriptContext_ReadByte(ctx);
-    ctx->data[v0] = ctx->data[v1];
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 604 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_604(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_604(ctx);
 }
 
 static BOOL ScrCmd_Unused_00A(ScriptContext *ctx)
 {
-    u8 *v0 = (u8 *)ScriptContext_ReadWord(ctx);
-    u8 *v1 = (u8 *)ScriptContext_ReadWord(ctx);
-    *v0 = *v1;
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 605 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_605(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_605(ctx);
 }
 
 static u32 Compare(u16 value0, u16 value1)
@@ -889,56 +890,56 @@
 
 static BOOL ScrCmd_Unused_00B(ScriptContext *ctx)
 {
-    u8 v0 = ctx->data[ScriptContext_ReadByte(ctx)];
-    u8 v1 = ctx->data[ScriptContext_ReadByte(ctx)];
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 606 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_606(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_606(ctx);
 }
 
 static BOOL ScrCmd_Unused_00C(ScriptContext *ctx)
 {
-    u8 v0 = ctx->data[ScriptContext_ReadByte(ctx)];
-    u8 v1 = ScriptContext_ReadByte(ctx);
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 607 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_607(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_607(ctx);
 }
 
 static BOOL ScrCmd_Unused_00D(ScriptContext *ctx)
 {
-    u8 v0 = ctx->data[ScriptContext_ReadByte(ctx)];
-    u8 v1 = *(u8 *)ScriptContext_ReadWord(ctx);
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 608 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_608(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_608(ctx);
 }
 
 static BOOL ScrCmd_Unused_00E(ScriptContext *ctx)
 {
-    u8 v0 = *(u8 *)ScriptContext_ReadWord(ctx);
-    u8 v1 = ctx->data[ScriptContext_ReadByte(ctx)];
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 609 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_609(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_609(ctx);
 }
 
 static BOOL ScrCmd_Unused_00F(ScriptContext *ctx)
 {
-    u8 v0 = *(u8 *)ScriptContext_ReadWord(ctx);
-    u8 v1 = ScriptContext_ReadByte(ctx);
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 729 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_729(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_729(ctx);
 }
 
 static BOOL ScrCmd_Unused_010(ScriptContext *ctx)
 {
-    u8 v0 = *(u8 *)ScriptContext_ReadWord(ctx);
-    u8 v1 = *(u8 *)ScriptContext_ReadWord(ctx);
-    ctx->comparisonResult = Compare(v0, v1);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 730 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_730(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_730(ctx);
 }
 
 static BOOL ScrCmd_CompareVarToValue(ScriptContext *ctx)
@@ -959,15 +960,11 @@
 
 static BOOL ScrCmd_Unused_013(ScriptContext *ctx)
 {
-    FieldSystem *fieldSystem = ctx->fieldSystem;
-    u8 *v2 = FieldSystem_GetScriptMemberPtr(fieldSystem, SCRIPT_MANAGER_NUM_ACTIVE_CONTEXTS);
-    ScriptContext **v3 = FieldSystem_GetScriptMemberPtr(fieldSystem, SCRIPT_MANAGER_SUB_CONTEXT);
-
-    u16 v0 = ScriptContext_ReadHalfWord(ctx);
-    *v3 = ScriptContext_CreateAndStart(fieldSystem, v0);
-    (*v2)++;
-
-    return TRUE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 783 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_783(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_783(ctx);
 }
 
 static BOOL ScrCmd_CallCommonScript(ScriptContext *ctx)
@@ -1018,45 +1015,29 @@
 
 static BOOL ScrCmd_Unused_017(ScriptContext *ctx)
 {
-    FieldSystem *fieldSystem = ctx->fieldSystem;
-
-    MapObject **v2 = FieldSystem_GetScriptMemberPtr(fieldSystem, SCRIPT_MANAGER_TARGET_OBJECT);
-    u8 v0 = ScriptContext_ReadByte(ctx);
-    s32 v1 = (s32)ScriptContext_ReadWord(ctx);
-
-    if (MapObject_GetLocalID(*v2) == v0) {
-        ScriptContext_Jump(ctx, (u8 *)(ctx->scriptPtr + v1));
-    }
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 596 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_596(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_596(ctx);
 }
 
 static BOOL ScrCmd_Unused_018(ScriptContext *ctx)
 {
-    u32 taskVal = FieldTask_GetDummy1CVal(ctx->task);
-    u8 val = ScriptContext_ReadByte(ctx);
-    s32 offset = (s32)ScriptContext_ReadWord(ctx);
-
-    if (taskVal == val) {
-        ScriptContext_Jump(ctx, (u8 *)(ctx->scriptPtr + offset));
-    }
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 597 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_597(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_597(ctx);
 }
 
 static BOOL ScrCmd_Unused_019(ScriptContext *ctx)
 {
-    FieldSystem *fieldSystem = ctx->fieldSystem;
-
-    int *v2 = FieldSystem_GetScriptMemberPtr(fieldSystem, SCRIPT_MANAGER_PLAYER_DIR);
-    u8 v0 = ScriptContext_ReadByte(ctx);
-    s32 v1 = (s32)ScriptContext_ReadWord(ctx);
-
-    if (*v2 == v0) {
-        ScriptContext_Jump(ctx, (u8 *)(ctx->scriptPtr + v1));
-    }
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 598 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_598(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_598(ctx);
 }
 
 static BOOL ScrCmd_Call(ScriptContext *ctx)
@@ -1855,6 +1836,12 @@
     u8 entryStringID = ScriptContext_ReadByte(ctx);
     u8 entryIndex = ScriptContext_ReadByte(ctx);
 
+    /* openmmo: the PC menu's TRAVEL row goes in ahead of SWITCH OFF
+     * (mods/openmmo/src/openmmo_travel.c). */
+    {
+        extern void openmmo_travel_menu_entry(void *menuManager, unsigned entryStringID);
+        openmmo_travel_menu_entry(*fieldMenuMan, entryStringID);
+    }
     FieldMenuManager_AddMenuEntry(*fieldMenuMan, entryStringID, entryIndex);
     return FALSE;
 }
@@ -1891,6 +1878,11 @@
         return FALSE;
     }
 
+    /* openmmo: a TRAVEL pick is noted here and acted on once the PC is off. */
+    {
+        extern void openmmo_travel_menu_selected(unsigned selected);
+        openmmo_travel_menu_selected(*selectedOptionPtr);
+    }
     return TRUE;
 }
 
@@ -2193,6 +2185,17 @@
 
         MapObject *object = MapObjMan_GetLocalMapObjByMovementType(fieldSystem->mapObjMan, MOVEMENT_TYPE_FOLLOW_PLAYER);
 
+        /* openmmo: HeartGold's LockAll lets its follower finish the step it
+         * is on before the scene freezes (mods/openmmo/src/openmmo_follow_move.c). */
+        {
+            extern int openmmo_follow_lock_all_take(FieldSystem *fs, MapObject *obj);
+            extern BOOL openmmo_follow_lock_all_wait(ScriptContext *ctx);
+            if (openmmo_follow_lock_all_take(fieldSystem, object)) {
+                ScriptContext_Pause(ctx, openmmo_follow_lock_all_wait);
+                return TRUE;
+            }
+        }
+
         if (object
             && SystemFlag_CheckHasPartner(SaveData_GetVarsFlags(fieldSystem->saveData)) == TRUE
             && MapObject_IsMoving(object) != FALSE) {
@@ -2472,15 +2475,11 @@
 
 static BOOL ScrCmd_Unused_06A(ScriptContext *ctx)
 {
-    FieldSystem *fieldSystem = ctx->fieldSystem;
-
-    MapObject *mapObj = MapObjMan_LocalMapObjByIndex(fieldSystem->mapObjMan, ScriptContext_GetVar(ctx));
-    u16 *v1 = ScriptContext_GetVarPointer(ctx);
-    u16 *v2 = ScriptContext_GetVarPointer(ctx);
-    *v1 = MapObject_GetX(mapObj);
-    *v2 = MapObject_GetZ(mapObj);
-
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 727 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_727(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_727(ctx);
 }
 
 static BOOL ScrCmd_GetPlayerDir(ScriptContext *ctx)
@@ -2524,6 +2523,16 @@
     MapObject *mapObj = MapObjMan_LocalMapObjByIndex(ctx->fieldSystem->mapObjMan, ScriptContext_GetVar(ctx));
     u16 movementType = ScriptContext_ReadHalfWord(ctx);
 
+    /* openmmo: HeartGold's scripts move the follower between its three
+     * gaits with this command, and those gaits are the follower's own
+     * (mods/openmmo/src/openmmo_follow_move.c). */
+    {
+        extern int openmmo_follow_take_movement_type(MapObject *obj, int type);
+        if (openmmo_follow_take_movement_type(mapObj, movementType)) {
+            return FALSE;
+        }
+    }
+
     MapObject_SwitchMovementType(mapObj, movementType);
     return FALSE;
 }
@@ -2544,10 +2553,11 @@
 
 static BOOL ScrCmd_Unused_06E(ScriptContext *ctx)
 {
-    MapObject *mapObj = MapObjMan_GetLocalMapObjByMovementType(ctx->fieldSystem->mapObjMan, MOVEMENT_TYPE_FOLLOW_PLAYER);
-
-    sub_020633C8(mapObj, 0xfe);
-    return FALSE;
+    /* openmmo: this slot is unused by every script this game ships and
+     * by every HeartGold script, so it carries HeartGold's follower
+     * command 698 (mods/openmmo/src/openmmo_follow_move.c). */
+    extern BOOL openmmo_scrcmd_follow_698(ScriptContext *ctx);
+    return openmmo_scrcmd_follow_698(ctx);
 }
 
 static BOOL ScrCmd_CountUniqueSealsInSealCase(ScriptContext *ctx)
@@ -3820,6 +3830,17 @@
     u16 v3 = ScriptContext_GetVar(ctx);
     u16 destVarID = ScriptContext_ReadHalfWord(ctx);
 
+    /* openmmo: the Contest Hall's link door. This client's group is its server
+     * and its queue (mods/openmmo/src/openmmo_contest_link.c). */
+    {
+        extern BOOL openmmo_contest_club_take(ScriptContext *ctx, int commType,
+            int contestType, int contestRank, u16 destVarID);
+
+        if (openmmo_contest_club_take(ctx, commType, v2, v3, destVarID)) {
+            return TRUE;
+        }
+    }
+
     CommClub_StartBattleClient(fieldSystem, commType, v2, v3);
     ctx->data[0] = destVarID;
     ScriptContext_Pause(ctx, sub_02042F74);
@@ -3850,6 +3871,17 @@
     u16 v3 = ScriptContext_GetVar(ctx);
     u16 destVarID = ScriptContext_ReadHalfWord(ctx);
 
+    /* openmmo: the Contest Hall's link door. This client's group is its server
+     * and its queue (mods/openmmo/src/openmmo_contest_link.c). */
+    {
+        extern BOOL openmmo_contest_club_take(ScriptContext *ctx, int commType,
+            int contestType, int contestRank, u16 destVarID);
+
+        if (openmmo_contest_club_take(ctx, commType, v2, v3, destVarID)) {
+            return TRUE;
+        }
+    }
+
     CommClub_StartBattleServer(fieldSystem, commType, v2, v3);
     ctx->data[0] = destVarID;
     ScriptContext_Pause(ctx, sub_0204300C);
@@ -4012,6 +4044,16 @@
     u16 species = ScriptContext_GetVar(ctx);
     u8 level = (u8)ScriptContext_GetVar(ctx);
 
+    /* openmmo: a fight is the server's to deal, so the command asks across
+     * instead of computing one (mods/openmmo/src/openmmo_static.c). */
+    {
+        extern BOOL openmmo_static_battle_take_wild(ScriptContext *ctx, u16 species, u8 level);
+
+        if (openmmo_static_battle_take_wild(ctx, species, level)) {
+            return TRUE;
+        }
+    }
+
     Encounter_NewVsSpeciesAtLevel(ctx->task, species, level, battleResultMaskPtr, FALSE);
     return TRUE;
 }
@@ -4022,6 +4064,16 @@
     u16 species = ScriptContext_GetVar(ctx);
     u8 level = (u8)ScriptContext_GetVar(ctx);
 
+    /* openmmo: a fight is the server's to deal, so the command asks across
+     * instead of computing one (mods/openmmo/src/openmmo_static.c). */
+    {
+        extern BOOL openmmo_static_battle_take_legendary(ScriptContext *ctx, u16 species, u8 level);
+
+        if (openmmo_static_battle_take_legendary(ctx, species, level)) {
+            return TRUE;
+        }
+    }
+
     Encounter_NewVsSpeciesAtLevel(ctx->task, species, level, battleResultMaskPtr, TRUE);
     return TRUE;
 }
@@ -4032,6 +4084,16 @@
     u16 species = ScriptContext_GetVar(ctx);
     u8 level = (u8)ScriptContext_GetVar(ctx);
 
+    /* openmmo: a fight is the server's to deal, so the command asks across
+     * instead of computing one (mods/openmmo/src/openmmo_static.c). */
+    {
+        extern BOOL openmmo_static_battle_take_giratina_origin(ScriptContext *ctx, u16 species, u8 level);
+
+        if (openmmo_static_battle_take_giratina_origin(ctx, species, level)) {
+            return TRUE;
+        }
+    }
+
     Encounter_NewVsGiratinaOrigin(ctx->task, species, level, battleResultMaskPtr, TRUE);
     return TRUE;
 }
@@ -4042,6 +4104,16 @@
     u16 species = ScriptContext_GetVar(ctx);
     u8 level = (u8)ScriptContext_GetVar(ctx);
 
+    /* openmmo: a fight is the server's to deal, so the command asks across
+     * instead of computing one (mods/openmmo/src/openmmo_static.c). */
+    {
+        extern BOOL openmmo_static_battle_take_fateful(ScriptContext *ctx, u16 species, u8 level);
+
+        if (openmmo_static_battle_take_fateful(ctx, species, level)) {
+            return TRUE;
+        }
+    }
+
     Encounter_NewFatefulVsSpeciesAtLevel(ctx->task, species, level, battleResultMaskPtr, TRUE);
     return TRUE;
 }
@@ -4145,6 +4217,24 @@
 
 static BOOL ScrCmd_131(ScriptContext *ctx)
 {
+    /* openmmo: the device may already be on, and then this must not run.
+     *
+     * The official client's "the Poketch switches on" task is written for the one state its
+     * script can be in: the lower screen is the unavailable placeholder. So it
+     * leaves that screen, PoketchUnavailableScreen_Exit, which is
+     * Heap_Destroy(HEAP_ID_POKETCH_APP), unloads that overlay and builds a
+     * PoketchSystem in its place. Run it with a PoketchSystem already live and
+     * it destroys the heap that system is holding and then creates a second
+     * system over the first, which is the crash a player meets when the
+     * Poketch Co president finally hands the watch over.
+     *
+     * A running device means there is nothing to switch on, so the command is
+     * the no-op it means. The RegisterPoketchApp calls either side of it in the
+     * script are untouched and still land. */
+    if (FieldSystem_GetPoketchSystem() != NULL) {
+        return FALSE;
+    }
+
     ov5_021DDBC8(ctx->task);
     return TRUE;
 }
@@ -6369,6 +6459,15 @@
     u16 *destVar = ScriptContext_GetVarPointer(ctx);
     FieldSystem *fieldSystem = ctx->fieldSystem;
 
+    /* What a smashed rock or a headbutted tree meets on a ported map is the
+     * server's roll, asked the moment the obstacle starts to go
+     * (mods/openmmo/src/openmmo_fieldmove.c). */
+    {
+        extern void openmmo_fieldmove_obstacle(FieldSystem *fs, int obstacle);
+
+        openmmo_fieldmove_obstacle(fieldSystem, (int)obstacle);
+    }
+
     switch (obstacle) {
     case 0:
         ov6_0224899C(fieldSystem, destVar, 0, HEAP_ID_FIELD3);
@@ -6379,6 +6478,11 @@
     case 2:
         ov6_0224899C(fieldSystem, destVar, 2, HEAP_ID_FIELD3);
         break;
+    case 3:
+        /* HeartGold's headbutted tree, the fourth row of the same table
+         * (mods/openmmo/src/openmmo_headbutt_effect.c). */
+        ov6_0224899C(fieldSystem, destVar, 3, HEAP_ID_FIELD3);
+        break;
     default:
         GF_ASSERT(FALSE);
         break;
