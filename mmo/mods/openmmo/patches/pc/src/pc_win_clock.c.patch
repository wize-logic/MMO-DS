--- a/pc/src/pc_win_clock.c	2026-09-03 01:24:32.415318190 -0400
+++ b/pc/src/pc_win_clock.c	2026-09-03 01:24:32.438761082 -0400
@@ -37,6 +37,7 @@
 #if defined(_WIN32)
 
 #define WIN32_LEAN_AND_MEAN
+#include <stdio.h>
 #include <windows.h>
 #include <time.h>
 
@@ -145,6 +146,14 @@
             }
             timer = CreateWaitableTimerA(NULL, FALSE, NULL);
         }
+        /* openmmo: which wait the pacer got, once, in the log, a run
+         * whose every trace row says slip 15 ms is on the tick, and this
+         * line says so before the rows do. */
+        fprintf(stderr, "pc-pace: %s\n",
+                ex != NULL && timer != NULL
+                    ? "high-resolution waitable timer"
+                    : (timer != NULL ? "1 ms tick and an ordinary timer"
+                                     : "no timer; Sleep() only"));
     }
     return timer;
 }
